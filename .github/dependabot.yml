version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      # Weekly so the SHA-pinned actions don't drift far behind upstream
      # security fixes (publish.yml holds PyPI publishing rights).
      interval: "weekly"
    commit-message:
      prefix: "ci"
